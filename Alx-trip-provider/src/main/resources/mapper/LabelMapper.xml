<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zb.mapper.LabelMapper">

    <select id="findfeatureall" resultType="com.zb.entity.ItripLabelDic">
        SELECT *FROM `itrip_label_dic` WHERE parentid=16
    </select>

    <select id="findBedtypeAll" resultType="com.zb.entity.ItripLabelDic">
        SELECT *FROM `itrip_label_dic` WHERE  parentid=1
    </select>


</mapper>